# Used only when running the API directly, outside Docker Compose.
EIP_DATABASE_URL=postgresql+asyncpg://eip:local-development-only@localhost:5432/eip
EIP_REDIS_URL=redis://localhost:6379/0

# Authenticated organization API budget per user.
EIP_API_USER_LIMIT=120
EIP_API_WINDOW_SECONDS=60
